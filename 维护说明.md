# 日语学习站 · 更新维护说明

## 🌐 正式线上地址（永久）

**https://ryuxuexi.netlify.app**

> 该站点由 Netlify 托管，2026-09-09 已确认线上主页面为最新 v2 版（版本戳与本地构建一致）。
> 注意：**线上还缺 3 个 PWA 文件**（sw.js / manifest.webmanifest / icon.svg 返回 404，见下方「补传 PWA 文件」），补传后即支持安装到手机桌面 + 离线使用。

## 文件结构

```
日语学习站/
├── index.html              模块化版入口（需配合 css/ js/ 目录）
├── build.js                ★ 打包脚本：把源码合成单文件 HTML
├── css/style.css           样式
├── js/app.js               ★ 核心引擎（SRS、变形引擎、路由、界面）
└── js/data/                ★ 数据层
    ├── kana.js             五十音 103 个（含字源、例词、9 组易混对）
    ├── vocab-n5.js         N5 528 词（新标日初级上 1-24 课）
    ├── vocab-n4.js         N4 500 词（新标日初级下 25-48 课）
    ├── vocab-n3.js         N3 176 词（8 主题）
    ├── vocab-n2.js         N2 200 词（8 主题）
    ├── vocab-n1.js         N1 178 词（8 主题）
    ├── verbs.js            动词 174 + 形容词 69 + 变形类型定义
    ├── grammar.js          语法 214 条（N5 55 / N4 46 / N3 40 / N2 39 / N1 34）
    └── audio-map.js        ★ 真人发音索引（由 tools/probe-audio.js 生成）
└── tools/probe-audio.js    ★ 真人发音索引生成脚本

日语学习站_dist/              部署用产物（由 build.js 生成，勿手改）
├── index.html               主页面（单文件，CSS/JS 全内联）
├── sw.js                    ★ Service Worker（离线缓存，须随 index.html 一起上传）
├── manifest.webmanifest     ★ PWA 清单（App 名称/图标/主题色）
└── icon.svg                 ★ App 图标
日语学习站_单文件版.html     本地双击版（与 dist 的 index.html 内容相同）
```

## 更新流程（改完内容后必做）

```bash
cd "C:\Users\Chen\Desktop\个人\日语学习站"
node build.js          # 重新打包 → 更新 dist/ 全部文件 和 单文件版
```

然后**把整个 `日语学习站_dist` 文件夹**部署/更新到 Netlify 站点（方法见下）。

### 更新 Netlify 站点（保留永久地址）

1. 登录 https://app.netlify.com → 打开站点 `ryuxuexi`
2. 进入 **Deploys** 标签页
3. 把**整个 `日语学习站_dist` 文件夹**（含 index.html + sw.js + manifest.webmanifest + icon.svg）拖进页面中的 "Drag and drop" 区域
4. 等 1-2 分钟部署完成，地址不变，仍是 https://ryuxuexi.netlify.app

> ⚠️ 一定要拖「文件夹」而不是只拖 index.html——否则 PWA 三件套（sw.js/manifest/icon.svg）会缺失，导致无法安装为 App、无图标、不能离线用。

### 补传 PWA 文件（首次缺失时用）

2026-09-09 检查发现线上只有 index.html，三个 PWA 文件 404。补传方法同上：登录 Netlify → `ryuxuexi` → Deploys → 拖整个 `日语学习站_dist` 文件夹覆盖一次即可。补传后可用下方命令自检：

```bash
# 三个都应返回 200（非 404）
curl -s -o /dev/null -w "%{http_code}\n" https://ryuxuexi.netlify.app/sw.js
curl -s -o /dev/null -w "%{http_code}\n" https://ryuxuexi.netlify.app/manifest.webmanifest
curl -s -o /dev/null -w "%{http_code}\n" https://ryuxuexi.netlify.app/icon.svg
```

### 本地预览（可选）

```bash
cd "C:\Users\Chen\Desktop\个人\日语学习站" && node build.js
# 双击生成在桌面的 日语学习站_单文件版.html 即可离线体验（PWA 功能仅 https 环境生效）
```

## 🔊 发音系统（三层音源，自动回落）

播放优先级由 `js/app.js` 的 `speak()` 决定：

1. **真人录音**（`js/data/audio-map.js` 中命中时）→ 直接播母语者 MP3，单词卡带绿色「真人」角标
   - 音源：JapanesePod101 公开词典音频（`assets.languagepod101.com`），**需联网**
   - 实测覆盖率约 **78%**（动词ます形基本不收录，脚本会自动转成基本形再查一次）
2. **TTS 神经语音**（未命中真人音、或读例句/句子时）→ 自动打分挑选设备最优日语语音：
   - `Google 日本語`（Chrome/Edge 云端神经语音，质量最好）> Win11 `Aoi/Naoki/Shiori` > `Ayumi/Ichiro/Haruka` > 其他
3. **用户手动指定**：仪表盘 → 发音设置 → 音源（自动/仅真人/仅TTS）+ 语音下拉 + 语速滑块

**未命中真人的词、例句、句子一律走 TTS**，不会静默失败。

### 真人音缓存（关键，绕过音源延迟）

音源实测（国内网络）：**延迟 7.6–9.2 秒，约 1/3 概率超时失败，且无 CORS 头**。所以：

- **页面端 fetch 缓存不可行**（跨域无 `Access-Control-Allow-Origin`，fetch 直接失败）
- **必须靠 Service Worker 拦截 `<audio>` 请求落缓存**：`sw.js` 里对 `languagepod101.com` 走 **cache-first**，可缓存跨域 opaque 响应，无需 CORS
- 页面端只做三件事：查询是否已缓存（`isCached`）、主动预热（`warmAudio`，用 `new Request(url,{mode:"no-cors"})`）、统计/清空

缓存后效果：**秒播、可离线**（配合 PWA）。

- 仪表盘 → 发音设置 → **真人音缓存**：可一键缓存 N5 / 当前目标等级 / 全部，显示已缓存数量，支持清空
- 单词卡会自动后台预热**下一张卡**的音频，连续刷卡几乎无感
- 缓存名：`jp-audio-v1`（与页面资源 `jp-study-v1` 分开，便于单独清空）

### 防「语言串台」的两条硬规则

1. **系统没有日语语音时，绝不朗读**。`tts()` 里 `bestVoice()` 返回 null 就直接 return 并提示安装语音包——否则浏览器会用中文语音念日语，就是所谓的「串台」
2. **真人音回落 TTS 只允许一次**。`playHuman()` 用 `settled` 标志保护：`onerror` / `onstalled` / 超时兜底共用同一把锁，避免多个回调同时触发导致真人音与 TTS 重叠出声（这是原实现的 bug：`a.onerror` 和 `play().catch()` 会双触发回落）
3. 未缓存时若 **2.5 秒**还没出声，先用 TTS 发声，音频继续后台加载供下次命中——不干等

> 代码约定：`speechSynthesis` 一律写成 `window.speechSynthesis`（裸调用在非浏览器环境会 ReferenceError，且会被 try/catch 静默吞掉，很难查）

### 重新生成真人发音索引

扩充词表后，重新扫描一遍哪些词有真人音频：

```bash
cd "C:\Users\Chen\Desktop\个人\日语学习站"
node tools/probe-audio.js     # 约 8-12 分钟，扫描 1500+ 词条，输出 js/data/audio-map.js
node build.js                 # 重新打包
```

脚本要点（踩过的坑，别改回去）：

- 服务端会返回 **301 重定向**，必须用 `fetch`（自动跟随），`https.get` 不会跟随 → 会全部判为未命中
- 未命中时服务端返回**固定的 52288 字节占位音频**（md5 前缀 `7e2c2f954ef6`），必须按 md5 过滤，否则会把占位音当真人音收录
- 并发数控制在 5 左右，过高会被限流

## 📝 我的词库（用户自定义内容）

用户可在网站 **「我的词库」** 页自行添加内容，无需改代码。存储位置：`localStorage` → `jp_studio_v1` → `S.custom`，**随进度备份一起导出/导入**。

**添加的条目会并入三处**（不是孤立分区）：

| 类型 | 并入位置 | 说明 |
|---|---|---|
| 单词 | 对应等级（N5-N1）的单词卡队列 | 与内置词一起 SRS 复习、一起统计等级进度；卡片上标注「我的词库」 |
| 动词（词性 动1/动2/动3） | 变形训练池 | 若填的是ます形会自动转基本形（たべます→たべる）；**建议直接填基本形** |
| 语法 | 语法库（按等级筛选可见） | 可标记已掌握、可发音 |

**批量导入格式**（每行一条，制表符/逗号/空格分隔，列数自动识别）：

```
假名  中文                      ← 2 列
假名  汉字  中文                ← 3 列
假名  汉字  中文  词性          ← 4 列
つくる  作る  做，制造  动1
```

也支持直接粘贴 JSON 数组（含 `k`/`w`/`z`/`p`/`lv` 字段）。以 `#` 开头的行为注释。重复条目（同等级+同假名+同汉字）自动跳过。

**开发者注意（改这块代码前必读）**：

- 内置数据是不可变的 `VOCAB_BASE` / `VERBS_BASE` / `GRAMMAR_BASE`，运行时视图 `VOCAB` / `VERBS` / `GRAMMAR` = 内置 + 自定义，由 `rebuildAll()` 重建
- **任何增删改自定义内容后必须调 `rebuildAll()`**（新增/删除/批量导入/清空词库/清空进度都要）
- 错词本映射 `VMAP` 是懒加载缓存，`rebuildAll()` 里置空失效——**不要在模块顶层用 IIFE 预构建依赖 VOCAB 的映射**（踩过坑：IIFE 早于 rebuildAll 执行，VOCAB 还是空对象 → 崩溃）
- 自定义词条 id 前缀：`cv-`（单词）/ `cg-`（语法），与内置 `N5-12` / `g38` 不冲突
- 旧存档兼容：启动时对 `S.settings` 深合并、`S.custom` 浅拷贝断开与 `DEF` 的引用（否则清空进度后自定义词会残留污染）

## 数据格式（扩充内容时照抄）

**N5 词表**（数组格式）：
```js
[课号, "假名", "汉字表记", "中文", "词性"]
// 词性：名 / 动1 / 动2 / 动3 / 形1 / 形2 / 副 / 连体 / 叹 / 助 / 接 / 代
[1, "ちゅうごくじん", "中国人", "中国人", "名"]
```

**N4-N1 词表**（对象格式）：
```js
{ l: 课号或主题号, k: "假名", w: "汉字表记", z: "中文", p: "词性" }
```

**动词**：`["读音", "汉字", 类型, "中文", "等级"]`，类型 `1`=一类(五段) `2`=二类(一段) `3`=三类
**形容词**：`["读音", "汉字", "形1/形2", "中文", "等级"]`
**语法**：`["等级", "课号/主题号", "条目", "接续", "意思", "例句", "翻译"]`

> 动词只需要给基本形读音 + 类型，11 种变形由 `js/app.js` 的 `conj()` 自动算出，扩充动词时不用手写变形。

## 已验证

- 变形引擎 30/30 单测通过（含 行く→行って、ある→ない、来る→こない、勉強する→べんきょうできる）
- 每个假名都带字源汉字 + 例词；9 组易混对带游戏/IT 助记词

## 已知限制

- 发音用浏览器内置 TTS（Web Speech API），Chrome/Edge 效果最好，Safari 需系统装日语语音包
- 进度存 localStorage：**换浏览器或清缓存会丢失**，重要进度建议在仪表盘页留意
- 纯静态站无后端：换设备进度不互通，需要时用仪表盘页的「导出/导入」手动备份迁移

## 备份方案

> 永久地址已在使用中：https://ryuxuexi.netlify.app （CloudStudio 沙箱链接仅作临时预览/备用）。

- CloudStudio 沙箱链接（`xxx.app.workbuddy.link`）是临时预览用，每次重新部署会变、可能过期
- 主阵地 = **Netlify 站点 ryuxuexi**（永久）。日常改完内容 → `node build.js` → 拖 `日语学习站_dist` 文件夹到 Netlify Deploys 覆盖即可
- 可选进阶：连 GitHub 仓库，Netlify 检测 push 自动构建发布（要自动发布可再配 CI）
