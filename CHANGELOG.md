# 更新记录（CHANGELOG）

本项目 2026-09-09 从零开始，一天内迭代了 4 个大版本。格式参考 Keep a Changelog，版本号按功能里程碑划分。

> **关于 Git 提交记录**：仓库 `lxrchen/ryuxuexi` 在 **1.4.0** 时才初始化，此前 1.0.0 – 1.3.0 均为本地开发（无版本控制）。因此旧版本没有对应的 commit hash，此处按实际开发时间记录。

---

## 项目脉络（为什么会有这个站）

| 阶段 | 内容 | 产出 |
|---|---|---|
| 起点 | 总结《新标日（第二版）初级上》PDF（扫描版，无文字层，靠渲染页面识别） | 全书结构、24 课语法主线、6 学时学习法 |
| 学习规划 | 针对「五十音平假名 OK、片假名需思考」的现状定制路径 | `片假名7天过关打卡表.md`（字源表、9 组易混对、24 个游戏/IT 外来语、9 处错处清单） |
| 产品化 | 把学习路径做成可交互网站 | 本站 |

贯穿全程的核心判断：**这本书本质是一套「动词变形系统」**——24 课约 70 个句型，九成是同一个动词换不同「形」。所以按「形」归并按规则推算，比零散记句型高效得多。

---

## [1.5.4] — 2026-09-11 · 队列数量透明化 + 修复课号筛选抽不到词

用户疑问：点「重建队列」后，本轮剩余数量为什么会是 2 / 1 / 0，是不是坏了？

### 查证结论：数量本身不是 bug，但有两个真问题

**① 信息不透明**——抽取数量 = `每日新词额度(默认20) − 今日已学`。已学 18 个时重抽就只有 2 个，学满 20 个就是 0 个。逻辑正确，但页面只显示「本轮剩余 2」，没说明原因，看起来像功能坏了。

**② 数量会跳变**（真不一致）——首次进入页面给 `min(额度, 10)`，但点「重抽本轮」给的是**全部剩余额度**（可能是 15 个）。同一动作两种结果，用户无法预期。

**③ 课号筛选抽不到词**（真 bug）——`buildQueue` 先随机取 10 个全级别新词、**再**按课号过滤，如果这 10 个里恰好没有该课的词，队列就空了。顺序反了。

### 修复

- **统一每轮最多 10 个**：`newList(st.lv, Math.min(quota, 10), st.lesson)`，首次进入与重抽结果一致，不再跳变
- **修 `newList(lv, n, lesson)`**：先按课号过滤候选池、再取数量（原先是反的）
- **新增 `queueNote()`**：在队列上方用一行话说明本轮数量的来源
  - 学习页额度不足：「本轮只有 2 个 —— 因为**今日新词额度只剩 2 个**（可在仪表盘调整）。本级别还有 486 个新词没学：[超额再学 10 个]」
  - 学习页额度充足：「本轮 10 个新词（都是你还没背过的）」
  - 按课号筛选：「本轮 3 个新词（已按课号筛选）」
  - 复习页：「本轮 2 个到期词 —— 这就是当前**所有**「已学过且到期」的词，走完就没有了；没背过的新词不会出现在这里」
- **统计行补充**：新增「今日额度剩余 N」，并把按钮改名为「重抽本轮」（比「重建队列」更能表达实际行为）
- `doneHTML()` 的剩余新词数也改为按当前课号统计

### 验证

v10 **24/24 通过**：额度剩 2/1/0 时队列数量与说明文案一致、额度充足时固定 10 个、重抽不再跳变、超额重抽可用、课号筛选能正常抽到词、复习页说明正确。回归 v9 18、v8 31、v7 30、v6 36、v5 27、v4 32（累计 198 项）。

---

## [1.5.3] — 2026-09-10 · 修复听写「看答案」重复堆叠

用户反馈：听写里点「看答案」，答案会一直往下叠加，实际只需要一个。

### 根因

「看答案」用的是 `insertAdjacentHTML("beforeend")` **追加**，但没有先清除旧节点——点 N 次就有 N 个答案块。而且每次都执行 `dct.w++`，**连点会把错误数刷高**，统计失真。

### 修复

- 抽出 `insertAnswer(v)`：插入前先 `querySelectorAll(".gex")` **清空所有旧答案块**，保证页面上永远只有一个
- 抽出 `answerHTML(v)` 复用答案块结构（原先两处各写一份，容易再走偏）
- 「看答案」加守卫 `if (!dct.q || dct.revealed) return;`——**揭示过就不再响应**，错误数只计一次
- `checkDict()` 也改用 `insertAnswer()`，提交答案时同样只保留一个

### 验证

v9 **18/18 通过**：连续插入 5 次后答案块始终为 1、换词后仍为 1、提交后唯一且内容与当前题一致、答案块结构正确。回归 v8 31、v7 30、v6 36、v5 27、v4 32（累计 174 项）。

---

## [1.5.2] — 2026-09-10 · 修复队列走完后的死状态

用户反馈：**新词学完后「重建队列」和「再来一轮」都点了没反应，还会连带复习界面失效**。

### 根因（两个叠加的 bug）

1. **`render()` 只在「队列为空」时重建队列**
   ```js
   if (!vs.queue.length) buildQueue(false);   // 旧逻辑
   ```
   队列非空但已刷完（`idx >= length`）时不重建 → 切回页面仍停在「本轮完成 🎉」，点「再来一轮」又因新词池为空而拿到空队列 → **看起来按钮坏了**。

2. **空队列时没有原因提示**
   学完新词后去复习页：新词的 `due` 是明天 → `dueList()` 为空 → 页面显示「本轮完成 🎉 + 再来一轮（无效）」，用户会以为**复习功能整个坏了**。

### 修复

- **重建时机**：新增 `queueJustSet` 标记——评分 / 手动重建 / 切换范围触发的 render 不重建；**进入页面时一律重建**（跨天、刷完、队列为空都能拿到最新内容）
- **收尾提示按原因区分**（`doneHTML()`）：

| 场景 | 提示 |
|---|---|
| 复习页：没学过任何词 | 「还没有学过任何词」+ 去学新词 |
| 复习页：学过但都没到期 | 「本轮复习完成」+ **下次复习：明天 09:30（还有 3 个在排）** |
| 学习页：今日额度用完 | 「额度已用完（20/20）」+ **「超额再学 10 个」**按钮 |
| 学习页：本级别学完 | 「本级别已全部学完 🎉」+ 去复习 |
| 学习页：还有词 | 「本轮完成」+ 还剩 N 个可学 + 再来一轮 |

- **新增「超额再学」**：`buildQueue(fresh, force)` 支持忽略每日新词额度，让用户自己决定继续
- `nextDueInfo()` / `fmtWhen()`：计算最近到期时间并格式化（约 N 分钟后 / 今天 HH:MM / 明天 HH:MM / M月D日）

### 验证

v8 **31/31 通过**：复习页三种空状态文案正确、未到期时给出下次复习时间、额度用完时给超额按钮且点得动、本级别学完提示、时间格式化四种情况。回归 v7 30、v6 36、v5 27、v4 32 全通过（累计 156 项）。

---

## [1.5.1] — 2026-09-10 · 学习与复习拆成两个独立页面

上一版只是在同一个页面里加了「模式切换」，本质上仍共用同一个队列容器 `vs`——切换时还是同一件事（甚至在同一路由下点击会因 hash 未变化而不刷新）。这次做成**物理分离**。

### 改动

- **两套独立状态**：`ls`（学习）与 `rs`（复习），各自维护级别、课号范围、队列、索引、翻面状态
  - 学习页切到 N4、复习页留在 N5，两边各自记住自己的设置
- **两个独立路由**：`#/learn`（学新词）、`#/review`（复习），导航分列两项；原「单词卡」入口取消，`#/vocab` 保留为兼容别名
- **队列语义写死**：
  - 复习 = `dueList()`，**只含「学过且到期」**，与未学词零交集
  - 学习 = `newList()`，**只含「从没背过」**
  - 去掉「混合」模式（那正是让人分不清的根源）
- **页面文案明确**：两页顶部各自说明「这里只会出现…」，并给一个跳到对方的按钮
- **修复刷新问题**：`data-go` 指向当前页时 hash 不变不触发 `hashchange`，现在会手动重建队列并 render

### 数据隔离验证

- 在学习页连刷 3 个词（写入 `S.cards`，due 设为未来）→ 复习队列长度**不变**
- 刚学的词 due 未到，不会立刻串进复习池
- 两个队列交集为 0，且数量之和 ≤ 该级别总词数

### 验证

v7 **30/30 通过**（两份状态互相独立、复习队列不含未学词、学习队列不含已学词、切换后指针正确、级别/课号各自独立、两页文案与互跳、回归）。v6（36）、v5（27）、v4（32）回归全通过。

---

## [1.5.0] — 2026-09-10 · 学习/复习拆分 + 听写与翻译练习 + 发音误导修复

### 修复：ます形被读成基本形（误导用户）

用户反馈：卡片上写着「寝ます」，发音却是「ねる」。

- 根因：为了让动词也能命中真人音，探测脚本把**ます形降级成基本形**去查词典（词典只收基本形）——确实命中了，但播出来的是另一个词
- 修复：**移除这 81 条转换条目**（`寝ます→寝る`、`来ます→くる`、`働きます→働く`…），真人音索引 1121 → **1040**
- ます形词条现在回落 TTS，读出的是真正的「ねます」
- 探测脚本里已加注释锁死该行为，防止重新生成时又把转换项加回来

> 注：`masuToDict()` 仍保留，但只用于**变形训练**（自定义动词的ます形需转基本形才能算变形）——那是正确用途，与发音无关。

### 新增：学习与复习彻底分开

此前「开始复习」和「学习新词」进的是同一个混合队��，用户分不清。现在三种模式：

| 模式 | 内容 |
|---|---|
| **学新词** | 只推没背过的词 |
| **复习** | 只推学过且到期的词 |
| **混合** | 两者都推（原行为） |

- 单词卡页顶部可切换，每个模式显示当前数量
- 首页两个入口分别直达对应模式

### 新增：听写练习（`#/dict`）

- 播放发音（真人优先）→ 写出单词，假名或汉字均可
- 判分归一化：去空格、全角转半角、统一小写，避免手滑被判错
- 支持慢速重听、看答案、下一题
- 可选级别 + 范围：**已学 / 本级全部 / 错词本**
- 答错自动进错词本，答对自动移出

### 新增：翻译练习（`#/trans`）

- **日 → 中**：看日文四选一选中文（干扰项同级别随机取，保证不重复）
- **中 → 日**：看中文拼写日文，自动判分
- 方向与级别可切换，统计正确率

### 验证

v6 测试 **39/39 通过**：ます形无真人音、普通词与基本形动词仍有真人音、三种模式队列互不交集、词池三种范围、判分归一化、选项无重复、内置功能回归。v5（27/27）、v4（32/32）回归同样通过。

---

## [1.4.0] — 2026-09-10 · 真人音缓存 + 修复语言串台 + 接入 Git 自动部署

`8be5ef2` · `c26612b` · `fa51f43`

### 修复（用户反馈：语言互窜、每次都要重新加载）

1. **真人音与 TTS 同时出声**
   - 根因：`playHuman()` 里 `a.onerror` 和 `play().catch()` 都会触发回落，音频一卡两个路径同时点火
   - 修复：引入 `settled` 单锁，`onerror` / `onstalled` / 超时兜底共用一把锁，**全程只回落一次**；`play().catch()` 不再触发回落

2. **系统没有日语语音时，用中文语音念日语**（用户听到的"串台"）
   - 修复：`bestVoice()` 返回 null 时**直接不朗读**，并提示安装语音包
   - 原则：宁可不出声，也不用错误语言的语音

3. **`speechSynthesis` 裸调用**
   - 在非浏览器环境会抛 ReferenceError，且被 `try/catch` 静默吞掉，极难排查
   - 统一改为 `window.speechSynthesis`

### 新增

- **音频缓存**（核心）
  - Service Worker 对 `languagepod101.com` 走 **cache-first**，缓存名 `jp-audio-v1`（与页面缓存分离，便于单独清空）
  - 缓存后**秒播、可离线**
  - 页面端：`isCached()` / `warmAudio()` / `cachedCount()` / `clearAudioCache()` / `warmLevel()`
  - 预热用 `new Request(url, { mode: "no-cors" })`——跨域资源无 CORS 头，只有这种方式能存入 Cache API
- **2.5 秒兜底**：未缓存时若 2.5 秒没出声，先用 TTS 发声，音频继续后台加载供下次命中
- **自动预热下一张卡**：单词卡渲染时后台预取下一张的音频，连续刷卡几乎无感
- **仪表盘「真人音缓存」面板**：一键缓存 N5 / 当前目标 / 全部，显示已缓存数量，支持清空
- **Git 仓库与自动部署**：`netlify.toml`（`command = node build.js`，`publish = dist`）、`README.md`、`.gitignore`
- `build.js` 输出改到**仓库内 `dist/`**，同时同步一份到仓库外 `日语学习站_dist/`（保留手动拖文件夹习惯）

### 关键实测数据（决定了技术方案）

| 项 | 实测 |
|---|---|
| 音源延迟 | 7.6 – 9.2 秒 |
| 失败率 | 3 次中 1 次超时 |
| CORS | **无** `Access-Control-Allow-Origin` |

→ 结论：页面端 fetch 缓存方案**不可行**，必须靠 Service Worker 拦截 `<audio>` 请求。

### 部署踩坑

远程仓库最初是 **Netlify 自动创建的初始提交**（旧版 4 个产物文件直接堆在根目录），与本地「源码 + dist 子目录」结构的根目录 `index.html` / `sw.js` 同名冲突，push 被拒（unrelated histories）。

解法（不丢历史、不强推）：

```bash
git fetch origin
git merge --allow-unrelated-histories -X ours origin/main -m "合入远程初始提交，保留本地结构"
git push -u origin main
```

### 验证

- **v5 测试 27/27 通过**：失败只回落一次、已出声后不重复回落、无日语语音不朗读、有日语语音优先 Google 日本語、缓存 CRUD、缓存不可用时安全降级、真人/TTS 端到端分流
- 线上四个文件 200，Netlify 自动构建生效

---

## [1.3.0] — 2026-09-09 · 我的词库（用户自定义内容）

### 新增

- **「我的词库」页面**（导航新增），三个页签：单词 / 语法 / 批量导入
- 自定义内容**并入**现有体系，而非孤立分区：
  - 单词 → 并入指定等级的单词卡队列，与内置词一起 SRS 复习、一起统计等级进度（卡片标注「我的词库」）
  - 动词（词性 动1/动2/动3）→ 自动进入变形训练池
  - 语法 → 并入语法库，可按等级筛到、可标记掌握
- **ます形自动转基本形**：`たべます → たべる`、`働きます → 働く`。优先用内置动词表反查，再用い段→う段规则兜底
- **批量导入**：制表符 / 逗号 / 空格分隔，2–4 列自动识别，支持粘贴 JSON，`#` 开头为注释，重复条目自动跳过
- **去重**：同等级 + 同假名 + 同汉字视为重复
- 词库随进度备份一起导出/导入；清空进度时询问是否保留

### 架构改造

内置数据改为不可变 `VOCAB_BASE` / `VERBS_BASE` / `GRAMMAR_BASE`，运行时视图 `VOCAB` / `VERBS` / `GRAMMAR` = 内置 + 自定义，由 `rebuildAll()` 重建——**原有 9 处 `VOCAB[L]` 调用点一行未改**。

### 修复的隐患

1. `VMAP`（错词本映射）原是模块顶层 IIFE，早于 `rebuildAll()` 执行 → `VOCAB` 还是空对象 → 崩溃。改为懒加载 `vmap()` + 重建时置空
2. 旧存档 `Object.assign({}, DEF, r)` 是浅拷贝 → `S.custom === DEF.custom` 共享引用，清空进度会污染默认值。启动时深合并 settings、浅拷贝 custom 断开引用
3. 清空进度后必须调 `rebuildAll()`，否则 VOCAB 仍含自定义词

### 验证

v4 测试 **32/32 通过**（并入队列、动词入池、ます形转换、去重、语法并入、删除后重建、三种分隔符批量解析、内置功能回归）

---

## [1.2.0] — 2026-09-09 · 发音系统（真人音源 + 智能 TTS 选音）

用户反馈：单词发音音色不好。

### 新增

- **真人录音库接入**：扫描全站 1512 个词条，逐个比对母语者录音库，生成索引 `js/data/audio-map.js`——**1121 条命中（74%）**
- **智能 TTS 选音**：自动打分挑选设备最优日语语音（Google 日本語 100 > Aoi/Naoki/Shiori 80 > Ayumi/Ichiro 20 > eSpeak −60）
- **仪表盘「发音设置」面板**：音源模式（自动 / 仅真人 / 仅 TTS）、语音下拉、两个语速滑块、试听按钮
- 单词卡命中真人音时显示绿色「真人」角标

### 探测脚本踩的三个坑（已固化进 `tools/probe-audio.js`）

1. 接口返回 **301 重定向**，`https.get` 不跟随 → 首轮探测 **0% 命中**；必须改用 `fetch`
2. 未命中时返回**固定 52288 字节占位音频**（MD5 前缀 `7e2c2f954ef6`），必须按指纹过滤，否则占位音会被当真人音收录
3. 词典不收录动词ます形 → 用 verbs.js 建「ます形→基本形」反查表，**多捞回 81 个词**

### 验证

v3 测试 **28/28 通过**

---

## [1.1.0] — 2026-09-09 · 交互与视觉大升级 + PWA

用户反馈：担心纯静态效果差、想要更多交互、界面更美观。

### 新增

- **全局快捷键**：`1-4` 选项/评分、`空格` 翻卡、`Enter` 下一题、`S` 发音
- **单词卡 3D 翻转**、答对弹动 / 答错抖动
- **游戏化**：连击 combo、XP、等级系统、9 枚成就徽章、Toast 提示
- **错词本**：评「忘记/困难」自动收集，一键重练
- **今日目标进度环**、**未来 7 天复习量预测**
- **可点击发音的五十音图**
- **数据安全**：进度导出 / 导入 JSON
- **PWA**：`manifest.webmanifest` + `sw.js` + `icon.svg`，可装到手机桌面、离线可用

### 视觉

CSS 全量重写：径向渐变背景、玻璃拟态顶栏、卡片层次与悬浮阴影、日文字体栈（Noto Sans JP）、渐变标题、hover 微动效、入场动画、移动端适配。

### 结论沉淀

纯静态**不影响**学习效果（Anki 同理）。学习效果取决于内容量、复习算法、是否坚持——与有无后端无关。真正短板只有三个：跨设备不同步（→ 导出导入）、无真人音频（→ 真人音库）、无自适应（→ 错词本 + 到期预测，本地即可实现）。

---

## [1.0.0] — 2026-09-09 · 首个版本

- 八大模块雏形：首页、五十音、单词卡、语法库、变形训练、仪表盘
- 内容：词汇 1582（N5/N4 完整，N3–N1 骨架）、语法 214 条、动词 174、形容词 69、假名 103
- 引擎：SM-2 间隔重复、动词 11 种 / 形容词 8 种变形自动推算（30/30 单测通过）、hash 路由、localStorage 持久化
- 构建：`build.js` 一键打包成单文件 HTML

### 同期产出

- `日语学习站/维护说明.md`：数据格式、更新流程、已知限制
- 自动化任务「日语学习站 · 每周内容扩充」：每周日自动补 N3/N2/N1 词表与语法 → 打包 → 校验 → 推送

---

## 版本命名规则

- **主版本**：架构级变更（如数据层重构、部署方式改变）
- **次版本**：新增功能模块
- **修订**：bug 修复与小优化

当前版本：**1.5.4**
